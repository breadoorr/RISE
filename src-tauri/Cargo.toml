[package]
name = "rise"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

[lib]
name = "rise_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "1.5.6", features = [] }

[dependencies]
tauri = { version = "1.8.3", features = ["dialog-all", "fs-all", "shell-open", "window-all"] }
serde = { version = "1.0.219" }
serde_json = "1"

[dev-dependencies]
rand = "0.8.5"

[features]
custom-protocol = ["tauri/custom-protocol"]
